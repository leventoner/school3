# Rust
/backend/target
/backend/Cargo.lock
# (Keep Cargo.lock for binary crates, but some prefer to ignore. Java project ignored mvn files?)
# Actually, the Java project had .mvn in list_dir so they committed it.

# Node / Angular
/frontend/node_modules
/frontend/dist
/frontend/.angular
/frontend/package-lock.json (Optional, but usually committed)

# IDEs
.idea
.vscode
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment files
.env
.env.local
.env.*.local
